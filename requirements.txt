# VPS Builder Worker Dependencies
numpy>=1.24.0,<2.0
boto3>=1.28.0
rich>=14.0.0

# FAISS (CPU only — index building is CPU-bound, GPU not needed)
faiss-cpu>=1.7.4

# ML (for torch.hub MegaLoc model loading — needed for feature dimension validation)
torch>=2.0.0
torchvision>=0.15.0
safetensors>=0.3.0
timm>=0.9.0
